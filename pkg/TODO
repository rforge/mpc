- Vectorize mpc() correctly! Currently gives a list() instead of "mpc"

Support matrix multiplication.

Vectorize as_mpc_numeric.

Add a vignette.

Write better man pages.

Comment the functions.

Add more tests.
